mutation createFunc{
  createFunc(moduleToken:"TestModule",name:"dada"){
    code
    message
  }
}

mutation updateFunc{
  updateFunc(id:1,name:"sasa"){
    code
    message
  }
}

mutation deleteFunc{
  deleteFunc(id:1){
    code
    message
  }
}

mutation set{
	setPermissions(id:1,permissionIds:[2,3,4]) {
	  code
	  message
	}
}